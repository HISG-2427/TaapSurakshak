[project]
name = "taapsurakshak-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "pydantic",
    "joblib",
    "numpy",
    "pandas",
    "scikit-learn",
    "xgboost"
]

[tool.vercel]
entrypoint = "backend.app.main:app"