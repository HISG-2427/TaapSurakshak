[project]
name = "taapsurakshak-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "joblib",
    "scikit-learn",
    "xgboost",
    "pandas"
]

[tool.vercel]
entrypoint = "backend.app.main:app"